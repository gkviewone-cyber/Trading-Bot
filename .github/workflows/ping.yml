name: Keep Render Awake (Market Hours)
on:
  schedule:
    - cron: '*/10 2-10 * * 1-5' 
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render URL
        run: curl https://trading-bot-qsjg.onrender.com/
